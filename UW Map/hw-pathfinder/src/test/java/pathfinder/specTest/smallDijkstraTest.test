#test finding a small least cost path

CreateGraph graph

AddNode graph A
AddNode graph B
AddNode graph C
AddNode graph D

AddEdge graph A B 1.0
AddEdge graph B C 2.0
AddEdge graph C D 1.0
AddEdge graph A D 10.0

FindPath graph A D